# Phase 5 Mega-Upgrade Task List

- [x] **1. Facebook/YouTube Stream Fix**
  - [ ] Implement `escape_tee_url` in `aibs_broadcast_daemon.py` to escape `|`, `[`, `]`, `\`, `:`, `=`, `,`, `%`.
- [x] **2. 6-Track Isolated Audio & Crash-Resilient MP4**
  - [ ] Update FFmpeg `tee` and `-filter_complex` audio mapping to mix Master Stream (Track 1) and isolated hardware streams (Tracks 2-6) in `aibs_broadcast_daemon.py`.
  - [ ] Inject `-movflags +faststart+frag_keyframe+empty_moov+default_base_moof` for crash resilience.
- [x] **3. NVENC Codecs & Tuning Presets**
  - [ ] Add Codec (`h264_nvenc`, `hevc_nvenc`, `av1_nvenc`) and Preset dropdowns in `BroadcastStudio.jsx` & `App.jsx`.
  - [ ] Backend routing for dynamic `-c:v` and `-preset` mapping.
- [x] **4. Vertical 9:16 Shorts Mode**
  - [x] Add 16:9 / 9:16 Canvas toggle in frontend.
  - [x] Update backend scaling/cropping filter for vertical outputs.
- [x] **5. DirectShow Virtual Webcam Output**
  - [x] Add Virtual Camera toggle in frontend.
  - [ ] Append `f=dshow:video="OBS Virtual Camera"` target to tee muxer output.
- [x] **6. Global Hotkeys**
  - [ ] Add `keydown` event listeners in `BroadcastStudio.jsx` for Ctrl+Shift+S, R, M, O, J.
