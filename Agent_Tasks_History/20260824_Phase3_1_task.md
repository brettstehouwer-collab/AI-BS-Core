# Tasks: Hybrid Pro Export Options

## 1. State Refactor
- [x] `dawStore.js`: Add `compositorNodes` and `compositorEdges` to the global store, with setter functions.
- [x] `NodeCompositor.jsx`: Remove local state and map `nodes` and `edges` to the global `dawStore.js` state.

## 2. Frontend Export UI
- [x] `MusicDAWStudioTab.jsx`: Update the "Pro Export" dropdown with new options: `fcpxml_raw`, `fcpxml_baked`, `aaf_raw`, `aaf_baked`.
- [x] `MusicDAWStudioTab.jsx`: Update the `handleProExport` function to bundle `compositorNodes` from the store into the POST payload.

## 3. Backend Translation
- [x] `export_router.py`: Update the `ExportPayload` model to accept `mode` (raw/baked) and `compositorNodes`.
- [x] `export_router.py`: Add XML generation logic to translate standard color and blur nodes into `<filter>` elements for `raw` mode.
- [x] `export_router.py`: Implement the `baked` mode branch (writing a proxy `<asset>` reference).

## 4. Documentation & Verification
- [ ] Update `AI_BS_MASTER_ARCHITECTURAL_LEDGER.md`
- [ ] Update `AI_BS_MASTER_ECOSYSTEM_MANUAL.md`
- [ ] Bump version numbers in UI and manuals.
- [ ] Firebase deployment `npm run build; firebase deploy`.
