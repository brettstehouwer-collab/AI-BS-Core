# 🎙️ Studio Quality TTS Upgrade Tasks

- `[x]` **Backend API Implementation (`backend/routers/screenwriting_router.py`)**
  - `[x]` Import `edge_tts` and `StreamingResponse`.
  - `[x]` Define Pydantic models for `StudioAudioPayload`.
  - `[x]` Implement `GET /api/screenwriting/studio_voices`.
  - `[x]` Implement `POST /api/screenwriting/generate_audio` (streaming generation).
- `[x]` **Frontend Integration (`frontend/components/ScreenplayTableReadModal.jsx`)**
  - `[x]` Remove standard `window.speechSynthesis` calls and initialization.
  - `[x]` Fetch available high-quality voices from backend.
  - `[x]` Modify `speakCurrentSegment()` to fetch blob from `POST /generate_audio`.
  - `[x]` Use `URL.createObjectURL(blob)` and an `Audio` object to play the audio.
  - `[x]` Map playback speed and pitch into valid `edge-tts` percentage strings (`+10%`, etc.).
  - `[x]` Assign default premium voices to roles (Narrator, etc.).
- `[ ]` **Verification & Deployment**
  - `[ ]` Test voice generation.
  - `[ ]` Test sequencing logic.
  - `[ ]` Build and deploy frontend.
