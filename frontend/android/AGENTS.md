# Android Studio Workspace Agent Instructions

When operating inside this directory (`C:\AI-BS\frontend\android`):
1. **Primary Directive:** Refer to `AGENT_DIRECTIVE_ANDROID_STUDIO.md` for complete build instructions, file path mappings, and compilation steps.
2. **App ID:** `com.stehouwerpublishing.digitalstorefront`
3. **Web Asset Location:** `app/src/main/assets/public`
4. **Target Outputs:**
   - Debug APK: `app/build/outputs/apk/debug/app-debug.apk`
   - Signed Release AAB: `app/build/outputs/bundle/release/app-release.aab`
