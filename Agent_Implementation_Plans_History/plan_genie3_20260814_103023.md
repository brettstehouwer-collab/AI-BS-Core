# Genie 3 Environment Integration Plan

We will scaffold an IPC bridge between your existing `AI_BS_Swarm_Commander.py` and a placeholder Genie 3 endpoint. Since Genie 3 is not fully publicly available via a standard REST API yet, we will construct the bridge to point to a local proxy/placeholder endpoint. Per your request, if this endpoint fails or is unresponsive, the system will automatically fall back to using Google Gemini (via the Antigravity SDK / Google Generative AI) to hallucinate and simulate the interactive environment state change.

## User Review Required

> [!WARNING]
> We will be adding a new column (`world_mutation`) to the `autonomous_actions` SQLite database to track what the Genie 3 / Gemini system decided to change in the Unreal environment. This requires an `ALTER TABLE` execution upon the next startup.
> Please review the architecture below and confirm if this aligns with your expectations.

## Open Questions

1. Do you have a specific local port or IPC endpoint that your Unreal Hubs currently listen to for environment changes? (I will default to `http://localhost:8888/genie/mutate` if none is provided).
2. For the Google Gemini fallback, would you prefer it to return a JSON payload with specific parameters (e.g., `{"weather": "rain", "lighting": "dark"}`) or a raw descriptive string?

## Proposed Changes

### AI-BS Core Daemon

#### [MODIFY] [AI_BS_Swarm_Commander.py](file:///C:/AI-BS/AI_BS_Swarm_Commander.py)
- **Database Schema Update:** Add `ALTER TABLE autonomous_actions ADD COLUMN world_mutation TEXT` in `init_actions_db()` to track the environment changes.
- **New Function `query_genie3_environment(industry, metric, llm_decision)`:** This function will `requests.post` to a placeholder Genie 3 endpoint.
- **New Function `query_gemini_fallback(prompt)`:** If the Genie 3 placeholder throws a ConnectionError or Timeout, this function will use the Google API (Gemini 3) to generate a simulated environment mutation (e.g., "The factory floor lighting shifts to red alert mode...").
- **Workflow Pipeline Update:** In `process_target`, immediately after `query_ollama` returns its strategic decision, that decision will be piped into `query_genie3_environment` to compute the visual/physical change required in the simulation. Both the strategic decision and the world mutation will be logged.

## Verification Plan

### Automated Tests
- I will run `python AI_BS_Swarm_Commander.py` briefly to ensure it parses correctly and the SQLite database is successfully altered without crashing.

### Manual Verification
- Once the Python daemon successfully processes a telemetry spike, we will check the SQLite database (`actions.db`) to ensure the `world_mutation` column contains either a Genie 3 placeholder response or the Google Gemini fallback generation.
